{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="image-accueil text-center">
        <div class="image-accueil-content">
            <h1>Connexion</h1>
            <div class="button-container">
                <button class="btn btn-warning" onclick="window.location.href = '/services#festindestination';">
                    </br>Le Festin Du Lion</br></br>
                    <img src="../images/icone_couverts.png" alt="Icone">
                </button>
                <button class="btn btn-warning" onclick="window.location.href = '/services#visitedestination';">
                    </br>Visite guidée</br></br>
                    <img src="../images/icone_chemin.png" alt="Icone">
                </button>
                <button class="btn btn-warning" onclick="window.location.href = '/services#traindestination';">
                    </br>L'Arcadia Tchou-Tchou</br></br>
                    <img src="../images/icone_train.png" alt="Icone">
                </button>
                <button class="btn btn-warning" onclick="window.location.href = '/#engagementsdestination';">
                    </br>Nos engagements</br></br>
                    <img src="../images/icone_eco.png" alt="Icone">
                </button>
            </div>
        </div>
    </div>

    <div class="container premiereLigne text-center">
        <h2>Seuls les employés d'Arcadia ont la possibilité de se connecter à notre site. Si vous vous êtes égaré sur cette page, un des boutons ci-dessus vous aidera à retrouver votre chemin</h2>

        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <form action="{{ path('app_login') }}" method="post" class="form-signin">
            <label for="username" class="sr-only"></label>
            <input type="text" id="username" name="_username" value="{{ last_username }}" required autofocus class="form-control" placeholder="Adresse mail">

            <label for="password" class="sr-only"></label>
            <input type="password" id="password" name="_password" required class="form-control" placeholder="Mot de passe">

            <input type="hidden" name="_target_path" value="/admin">

            <button class="btn btn-lg btn-primary btn-block mt-4" type="submit">Connexion</button>
        </form>
    </div>

    <!-- Modal -->
    <div id="myModal" class="modal">
        <div class="modal-content">
            <span class="close">×</span>
            <p class="modal-text">Seuls les salariés d'Arcadia peuvent se connecter sur notre site. Si vous êtes un simple visiteur, veuillez <a href="/" id="redirect-link">cliquer ici</a> pour retourner sur la page d'accueil.</p>
        </div>
    </div>

    <style>
        .modal {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgb(0,0,0);
            background-color: rgba(0,0,0,0.4);
        }

        .modal-content {
            background-color: #fefefe;
            margin: 15% auto;
            padding: 20px;
            border: 1px solid #888;
            width: 80%;
            max-width: 500px;
            text-align: center;
        }

        .modal-text {
            color: #37240E; /* Ajout de la couleur de la police */
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: black;
            text-decoration: none;
            cursor: pointer;
        }
    </style>

    <script>
        window.onload = function() {
            var modal = document.getElementById("myModal");
            var span = document.getElementsByClassName("close")[0];
            var redirectLink = document.getElementById("redirect-link");

            modal.style.display = "block";

            span.onclick = function() {
                modal.style.display = "none";
            }

            window.onclick = function(event) {
                if (event.target == modal) {
                    modal.style.display = "none";
                }
            }

            redirectLink.onclick = function() {
                window.location.href = "/";
            }
        };
    </script>
{% endblock %}
